If you are already running Simple IoT, run `siot update` to update to this
release.

Otherwise, download the binary for your platform, make it executable, and run
it. See the [documentation](https://docs.simpleiot.org) for installation
details.
